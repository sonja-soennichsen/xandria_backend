import { createContext } from "../../src/config/createContext"

describe("testing createContext function", () => {
  it("throws an error with invalid request", () => {
    // createContext()
    expect(true).toBe(true)
  })
})
